import type { RecordRow } from '../types.js';
import type { RawRecord } from './parse.js';

export function normalizeRecords(did: string, raw: RawRecord[]): RecordRow[] {
  throw new Error('stub');
}
